{% extends 'base.html.twig' %}

{% block title %}Proffessor information{% endblock %}

{% block body %}
    <h1>Proffessor information</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ user.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prof_index') }}">back to list</a>

    <a href="{{ path('app_prof_edit', {'id': user.id}) }}">edit role </a>

    {{ include('prof/_delete_form.html.twig') }}
{% endblock %}
